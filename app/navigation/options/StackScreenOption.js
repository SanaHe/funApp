import * as React from 'react';
import Colors from '../../constants/Colors';

const StackScreenOption = {
    headerTitleStyle:{
        fontWeight:'900',
        color:Colors.headerTitle
    },
    headerTintColor:Colors.primary
}

export default StackScreenOption
